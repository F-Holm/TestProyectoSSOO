name: Test Suite

on:
  pull_request:
    branches: 
      - main

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Setup
        run: sudo ./setup.sh

      - name: Tests
        run: make test
